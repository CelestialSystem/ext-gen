{
  "name": "ext-gen",
  "version": "7.8.0",
  "description": "ext-gen root",
  "main": "index.js",
  "scripts": {
    "increment": "npx lerna version --no-push --yes",
    "clean": "rm -rf node_modules && npx lerna clean",
    "install:clean": "npm cache clean --force && npm run clean && npm install"
  },
  "keywords": [
    "Ext",
    "JS",
    "Sencha"
  ],
  "author": "Sencha, Inc.",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^8.1.9"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sencha/ext-allshared"
  },
  "workspaces": [
    "packages/*"
  ]
}
